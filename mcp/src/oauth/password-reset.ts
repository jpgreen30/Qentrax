/** Supabase Auth password-reset helpers for MCP OAuth login. */
import { createClient } from "@supabase/supabase-js";
function sb(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL??process.env.SUPABASE_URL??"";const key=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY??process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY??process.env.SUPABASE_ANON_KEY??"";if(!url||!key)throw new Error("Supabase URL/key required");return createClient(url,key,{auth:{persistSession:false,autoRefreshToken:false}})}
export function supabasePublicConfig(){const url=process.env.NEXT_PUBLIC_SUPABASE_URL??process.env.SUPABASE_URL??"";const anonKey=process.env.NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY??process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY??process.env.SUPABASE_ANON_KEY??"";return{url,anonKey}}
export function passwordResetRedirectUrl(base:string){return `${base.replace(/\/$/,"")}/oauth/reset-password`}
export async function requestPasswordReset(email:string,base:string):Promise<{ok:true}|{ok:false;message:string}>{const trimmed=email.trim().toLowerCase();if(!trimmed||!trimmed.includes("@"))return{ok:false,message:"Enter a valid email address."};try{const{error}=await sb().auth.resetPasswordForEmail(trimmed,{redirectTo:passwordResetRedirectUrl(base)});if(error&&/rate|limit|too many/i.test(error.message))return{ok:false,message:"Too many reset attempts. Please try again later."};return{ok:true}}catch{return{ok:true}}}

import Link from "next/link";
const tenants = [{name:"Northstar Acquisition",type:"Advertiser",status:"Approved"},{name:"SignalWorks Media",type:"Publisher",status:"Approved"},{name:"Qentrax Operations",type:"Platform",status:"Active"}];
export default function Workspace(){return <main><nav><Link className="brand" href="/">QENTRAX</Link><span className="pill">Foundation preview</span></nav><section className="workspace"><p className="eyebrow">ORGANIZATION CONTEXT</p><h1>Choose your workspace</h1><p className="lede">Phase 0 fixture organizations exercise organization-scoped navigation. Supabase authentication is connected when environment credentials are configured.</p><div className="tenant-list">{tenants.map(t=><button key={t.name}><span><strong>{t.name}</strong><small>{t.type}</small></span><em>{t.status}</em></button>)}</div><p className="notice">Local preview data only — not a production identity session.</p></section></main>}

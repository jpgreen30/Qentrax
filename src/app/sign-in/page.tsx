import { requestMagicLink } from "./actions";
export default async function SignIn({searchParams}:{searchParams:Promise<{sent?:string;error?:string}>}){const state=await searchParams;return <main><section className="workspace narrow"><p className="eyebrow">SECURE ACCESS</p><h1>Sign in to Qentrax</h1><p className="lede">Use a secure email magic link to access your organization workspace.</p><form action={requestMagicLink}><label>Email<input name="email" type="email" autoComplete="email" required placeholder="you@company.com"/></label><button className="button">Continue securely</button></form>{state.sent&&<p className="notice" role="status">Check your inbox for the secure sign-in link.</p>}{state.error&&<p className="notice" role="alert">We could not start sign-in. Verify the email and try again.</p>}</section></main>}
